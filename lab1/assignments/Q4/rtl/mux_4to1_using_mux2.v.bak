module mux_4to1_using_mux2(
    input i0, i1, i2, i3,
    input s1, s0,
    output y
);

wire w1, w2;

// First stage
mux2to1 M1(i0, i1, s0, w1);
mux2to1 M2(i2, i3, s0, w2);

// Second stage
mux2to1 M3(w1, w2, s1, y);

endmodule